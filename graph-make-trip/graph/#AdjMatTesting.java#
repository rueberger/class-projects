package graph;

import org.junit.Test;
import ucb.junit.textui;
import static org.junit.Assert.*;

public class AdjMatTesting {
    
    private UndirectedGraph u;

    @Test
    public void testAdd() {
        u = new UndirectedGraph();
        assertEquals("
    }

}